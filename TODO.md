# TODO

- [ ] Scan `signup.html` and client script to see existing signup validation.
- [ ] Add password validation rule to block passwords that are only numbers.
- [x] Enforce minimum password length (>= 6) per requirement (and align with existing checks).

- [ ] Update UI/validation messages for password rule failures.
- [ ] Ensure the same rules are enforced server-side (optional but recommended).
- [ ] Run a quick sanity check by executing the signup flow in browser / unit check.

